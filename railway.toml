[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Port binding is handled by Dockerfile CMD (shell form expands $PORT).
healthcheckPath = "/api/v1/health"
restartPolicyType = "ON_FAILURE"
